import React from 'react';
import { View, Text } from 'react-native';
import { Screen } from '../../components';

export const Home = () => {
  return (
    <Screen>
      <View>
        <Text>Home</Text>
      </View>
    </Screen>
  );
};
