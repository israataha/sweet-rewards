import React from 'react';
import { View, Text } from 'react-native';
import { Screen } from '../../components';

export const Earn = () => {
  return (
    <Screen>
      <View>
        <Text>Earn</Text>
      </View>
    </Screen>
  );
};
